import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Lokasi = () => {
    return (
        <View>
            <Text>Lokasi</Text>
            {/* Isi dengan konten untuk halaman Lokasi */}
        </View>
    );
};


export default Lokasi

const styles = StyleSheet.create({})